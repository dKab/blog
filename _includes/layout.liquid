<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1"> 
        <title>My Blog</title>
    </head>
    <body>
        <main>
            <h1>{{ pageTitle }}</h1>
            {%- if page.url != '/' -%}
                 <a href="/">&#8592; Home</a>
            {%- endif -%}
            {{ content }}
        </main>
    </body>
</html>